import Calendar from "../../features/calendar/components/Calendar";

export default function Dashboard() {
    return (
        <main>
            <h1>Dashboard</h1>
            <Calendar />
        </main>
    );
}
